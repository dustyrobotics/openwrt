--- a/mac.c
+++ b/mac.c
@@ -69,6 +69,13 @@ static struct ieee80211_supported_band mors_band_5ghz = {
 	.n_bitrates = ARRAY_SIZE(mors_rates),
 };

+/* Global variables for TSF synchronization */
+static u64 last_rx_hw_tsf_us = 0;
+static u64 last_rx_kernel_time_ns = 0;
+static DEFINE_SPINLOCK(tsf_sync_lock);
+
+/* Forward declaration for TSF sync function */
+static void morse_capture_rx_tsf(const struct morse_skb_rx_status *hdr_rx_status);

 void morse_mac_process_rx_noa(struct ieee80211_vif *vif, struct sk_buff *skb, u8 *s1g_ie)
 {
@@ -5886,6 +5893,40 @@ static inline void morse_mac_preprocess_s1g_action(struct morse *mors,
 	}
 }

+/**
+ * morse_capture_rx_tsf - Capture TSF timestamp from received frames
+ * @hdr_rx_status: RX status containing hardware TSF timestamp
+ *
+ * Safely stores the hardware TSF timestamp and corresponding kernel time
+ * for clock synchronization purposes. This captures ALL received management
+ * and beacon frames to ensure frequent updates.
+ */
+static void morse_capture_rx_tsf(const struct morse_skb_rx_status *hdr_rx_status)
+{
+	unsigned long flags;
+
+	if (!hdr_rx_status)
+		return;
+
+	spin_lock_irqsave(&tsf_sync_lock, flags);
+	last_rx_hw_tsf_us = le64_to_cpu(hdr_rx_status->rx_timestamp_us);
+	last_rx_kernel_time_ns = ktime_get_real_ns();
+	spin_unlock_irqrestore(&tsf_sync_lock, flags);
+}
+
+/**
+ * morse_get_tsf_snapshot - Get atomic snapshot of TSF values
+ * @hw_tsf_us: Output - hardware TSF in microseconds
+ * @kernel_time_ns: Output - kernel time in nanoseconds
+ */
+void morse_get_tsf_snapshot(u64 *hw_tsf_us, u64 *kernel_time_ns)
+{
+	unsigned long flags;
+	spin_lock_irqsave(&tsf_sync_lock, flags);
+	*hw_tsf_us = last_rx_hw_tsf_us;
+	*kernel_time_ns = last_rx_kernel_time_ns;
+	spin_unlock_irqrestore(&tsf_sync_lock, flags);
+}

 static int morse_mac_process_s1g_mgmt_or_beacon(struct morse *mors,
 						struct ieee80211_vif *vif,
@@ -5903,6 +5944,10 @@ static int morse_mac_process_s1g_mgmt_or_beacon(struct morse *mors,
 	u32 ies_mask;
 	int ret = 0;

+	/* Capture TSF timestamp for clock synchronization from ALL frames */
+	morse_capture_rx_tsf(hdr_rx_status);
+
+	/* Continue with normal frame processing */
 	fc = morse_hdr_s1g->frame_control;
 	ies_mask = morse_mac_s1g_ie_get_mask(morse_hdr_s1g);

--- a/debug.c
+++ b/debug.c
@@ -39,6 +39,9 @@
 #define HOSTIF_LOG_BUFFER_SIZE 10000
 #define HOSTIF_LOG_MAX_LEN 500

+/* External TSF sync function from mac.c */
+extern void morse_get_tsf_snapshot(u64 *hw_tsf_us, u64 *kernel_time_ns);
+

 struct hostif_log_entry {
 	char msg[HOSTIF_LOG_MAX_LEN];
@@ -1034,6 +1037,68 @@ static const struct file_operations watchdog_fops = {
 	.llseek = generic_file_llseek,
 };

+/**
+ * tsf_rx_show - Display last received frame's TSF
+ *
+ * Shows the TSF timestamp captured from the last received management/beacon frame.
+ * This is used by CLIENTS (Gateway/Station) to determine when a packet arrived.
+ *
+ * Output format:
+ *   TSF: <hardware_tsf_us>
+ *   SYS: <kernel_time_ns>
+ */
+static int tsf_rx_show(struct seq_file *m, void *v)
+{
+	u64 hw_tsf_us, kernel_time_ns;
+
+	morse_get_tsf_snapshot(&hw_tsf_us, &kernel_time_ns);
+
+	seq_printf(m, "TSF: %llu\n", hw_tsf_us);
+	seq_printf(m, "SYS: %llu\n", kernel_time_ns);
+
+	return 0;
+}
+
+DEFINE_SHOW_ATTRIBUTE(tsf_rx);
+
+/**
+ * tsf_current_show - Display extrapolated current TSF
+ *
+ * Calculates the current TSF by extrapolating from the last received frame.
+ * Formula: current_tsf = last_rx_tsf + (current_time - last_rx_time)
+ *
+ * This is used by APs (Robot/Master) to get the current hardware time
+ * for broadcasting in time sync packets.
+ *
+ * Output format:
+ *   TSF: <current_tsf_us>
+ *   SYS: <current_kernel_time_ns>
+ *
+ * Note: TSF increments at 1 MHz (1 tick per microsecond), so we can
+ * accurately extrapolate by adding the time delta.
+ */
+static int tsf_current_show(struct seq_file *m, void *v)
+{
+	u64 last_hw_tsf_us, last_kernel_time_ns;
+	u64 current_kernel_time_ns;
+	u64 delta_ns, delta_us;
+	u64 current_tsf_us;
+
+	/* Get last captured values */
+	morse_get_tsf_snapshot(&last_hw_tsf_us, &last_kernel_time_ns);
+
+	/* Get current kernel time */
+	current_kernel_time_ns = ktime_get_real_ns();
+
+	/* Calculate time delta and extrapolate TSF */
+	delta_ns = current_kernel_time_ns - last_kernel_time_ns;
+	delta_us = delta_ns / 1000;  /* Convert ns to us */
+	current_tsf_us = last_hw_tsf_us + delta_us;
+
+	seq_printf(m, "TSF: %llu\n", current_tsf_us);
+	seq_printf(m, "SYS: %llu\n", current_kernel_time_ns);
+
+	return 0;
+}
+
+DEFINE_SHOW_ATTRIBUTE(tsf_current);

 static void morse_debug_fw_init(struct morse *mors)
 {
@@ -1161,6 +1224,14 @@ static void morse_debug_fw_init(struct morse *mors)
 	debugfs_create_file("ps_offload_data", 0600, mors->debug.debugfs_phy, mors,
 			    &ps_offload_fops);

+	/* TSF synchronization interfaces */
+	debugfs_create_file("tsf_rx", 0444, mors->debug.debugfs_phy, mors,
+			    &tsf_rx_fops);
+
+	debugfs_create_file("tsf_current", 0444, mors->debug.debugfs_phy, mors,
+			    &tsf_current_fops);
+
+
 	morse_debug_init_logging(mors);
 #endif
 }
